{% set mollie_subscription = hookable_metadata.context.resource %}
{% from '@SyliusAdmin/order/macro/order_payment_state_label.html.twig' import label %}

<div class="row mb-2 border-bottom pb-2">
    <div class="col-md-4 fw-bold">{{ 'sylius.ui.payment_state'|trans }}</div>
    <div class="col-md-8">
        {{ label(mollie_subscription.paymentState) }}
    </div>
</div>
